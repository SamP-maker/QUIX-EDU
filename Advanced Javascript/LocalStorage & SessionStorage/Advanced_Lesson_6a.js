// ========================================
// Module 6: LocalStorage & SessionStorage
// ========================================
// - Difference between localStorage and sessionStorage
// - Methods: setItem(), getItem(), removeItem()
// - Using JSON.stringify & parse
// - Practice: Theme switcher w/ saved state

/*
| Feature         | `localStorage`                    | `sessionStorage`                 |
| --------------- | --------------------------------- | -------------------------------- |
| Scope           | Persistent (even after tab close) | Temporary (cleared on tab close) |
| Lifetime        | Stays until manually removed      | Cleared when tab/browser closes  |
| Capacity        | \~5MB                             | \~5MB                            |
| Accessible From | Same origin (protocol + domain)   | Same origin, same tab only       |
*/
